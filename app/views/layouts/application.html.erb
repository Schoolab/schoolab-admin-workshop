<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=Edge,chrome=1">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "Schoolapp Admin" %></title>
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag "application", :media => "all" %>

    <%= javascript_include_tag "application" %>

    <!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
    <!--[if lt IE 9]>
    <script src="//cdnjs.cloudflare.com/ajax/libs/html5shiv/3.7.2/html5shiv.min.js" type="text/javascript"></script>
    <![endif]-->
  </head>
  <body>

    <div class="navbar navbar-schoolab navbar-static-top">
      <div class="container">
        <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-responsive-collapse">
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </button>
        <a class="navbar-brand" href="/">Schoolapp Admin</a>
        <div class="navbar-collapse collapse navbar-responsive-collapse">
          <ul class="nav navbar-nav">
            <% if user_signed_in? && current_user.role == "admin" %>
            <li><%= link_to "Événements", events_path  %></li>
            <li class="dropdown">
              <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Résidents <span class="caret"></span></a>
              <ul class="dropdown-menu">
                <li><%= link_to "Personnes", residents_path %></li>
                <li><%= link_to "Entreprises", companies_path  %></li>
              </ul>
            </li>
            <li class="dropdown">
              <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Bâtiment <span class="caret"></span></a>
              <ul class="dropdown-menu">
                <li><%= link_to "Liste des étages", floors_path  %></li>
                <li><%= link_to "Liste des salles", meeting_rooms_path  %></li>
              </ul>
            </li>
            <li class="dropdown">
              <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Salles de réunion <span class="caret"></span></a>
              <ul class="dropdown-menu">
                <li><%= link_to "Réservations", reservations_path  %></li>
                <li><%= link_to "Réserver une salle", search_reservations_path %></li>
              </ul>
            </li>
            <% elsif user_signed_in? && current_user.role == "user" %>
            <li><%= link_to "Réserver une salle", search_reservations_path %></li>
            <% end %>
          </ul>
          <!-- Right -->
          <ul class="nav navbar-nav navbar-right">
            <% unless user_signed_in? %>
              <li><%= link_to "Log in", new_user_session_path %></li>
              <li><%= button_to "Sign up", new_user_registration_path, method: :get, class: 'btn btn-info navbar-btn btn-header-signup' %></li>
            <% else %>
              <li class="dropdown">
                <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">
                  <%= current_user.first_name %>

                  <% if current_user.photo.file? %>
                    <div class="img-thumbnail img-square navbar-thumbnail">
                      <%= image_tag current_user.photo.url(:thumb), size: 32 %>
                    </div>
                  <% end %>
                </a>
                <ul class="dropdown-menu">
                  <li><%= link_to "Voir mon profil", resident_path(current_user) %></li>
                  <li><%= link_to "Paramètres", edit_user_registration_path %></li>
                  <li role="separator" class="divider"></li>
                  <li><%= link_to "Sign out", destroy_user_session_path, :method => :delete %></li>
                </ul>
              </li>
            <% end %>
          </ul>
        </div>
      </div>
    </div>

    <div class="container">
      <%= bootstrap_flash %>
      <%= yield %>

      <hr>

      <footer>
        <p>&copy; Schoolab 2016</p>
      </footer>

    </div> <!-- /container -->

  </body>
</html>
